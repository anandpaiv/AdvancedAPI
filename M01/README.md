# M01 — Python Foundations with OOP

Small domain model to practice:
- Encapsulation (properties, “private” fields)
- Invariants (`start < end`)
- Simple validator + factory patterns

## Run
```bash
python M01/main.py